# BookCatalogService - Мини веб-служба и конвейер обработки запросов

Этот проект представляет собой мини веб-службу на ASP.NET Core, которая демонстрирует принципы построения конвейера обработки HTTP-запросов. В рамках проекта реализованы базовые CRUD-операции для каталога книг, а также сквозные функции (логирование, замер времени, стандартизированная обработка ошибок) с использованием механизма Middleware.

## Предметная область: Каталог Книг

Веб-служба предоставляет API для управления коллекцией книг. Каждая книга имеет следующие атрибуты:

*   `Id` (GUID): Уникальный идентификатор книги.
*   `Title` (string): Название книги.
*   `Author` (string): Автор книги.
*   `PublicationYear` (int): Год публикации книги.

### Правила предметной области:

*   `Title` и `Author` являются обязательными полями и не могут быть пустыми.
*   `PublicationYear` должен быть корректным годом (например, между 1000 и текущим годом).

## Инструкция по запуску проекта

Для запуска проекта вам потребуется:

1.  **Установленный .NET SDK (версия 7.0 или выше):**
    *   Вы можете скачать его с официального сайта: [https://dotnet.microsoft.com/download](https://dotnet.microsoft.com/download)
    *   Убедитесь, что `dotnet --version` возвращает версию 7.x.x или 8.x.x.
2.  **Редактор кода (рекомендуется Visual Studio Code):**
    *   Visual Studio Code: [https://code.visualstudio.com/download](https://code.visualstudio.com/download)
    *   Расширение C# Dev Kit для VS Code.

### Шаги для запуска:

1.  **Клонируйте репозиторий** (или распакуйте архив проекта) в выбранную вами директорию.
2.  **Перейдите в папку проекта `BookCatalogService`** через терминал (командную строку):
    ```
    cd C:\Users\eliza\frameworks1\BookCatalogService
    ```
3.  **Запустите проект:**
    ```
    dotnet run
    ```
    После запуска в терминале вы увидите информацию о том, на каком адресе и порту доступно ваше приложение. В данном случае, сервис доступен по HTTP:
    ```
    info: Microsoft.Hosting.Lifetime[14]
          Now listening on: http://localhost:5111
    ```
    **Обратите внимание:** Для целей этого задания приложение настроено только на HTTP. Все команды `curl` и тестирование должны использовать `http://localhost:5111`.

### Доступ к Swagger UI

После запуска приложения, вы можете открыть интерактивную документацию API (Swagger UI) в браузере по адресу:
[http://localhost:5111/swagger](http://localhost:5111/swagger)

Swagger UI позволяет удобно просматривать доступные эндпоинты, отправлять запросы и видеть ответы.

## Описание API и Набор Проверок

Все API-эндпоинты начинаются с `/api/books`.

### 1. Получение списка всех книг

*   **Метод:** `GET`
*   **URL:** `http://localhost:5111/api/books`
*   **Описание:** Возвращает JSON-массив всех книг, хранящихся в памяти.
*   **Ожидаемый результат:** `200 OK` и список книг.

**Команда `curl`:**
```
curl -X GET http://localhost:5111/api/books
```

### Архитектурное пояснение: Аккуратный каркас

В данном проекте реализован подход к построению веб-службы, который обеспечивает чистоту кода, масштабируемость и легкость в сопровождении. Ключевая идея заключается в разделении ответственности и использовании механизма Middleware (промежуточного ПО) в ASP.NET Core.

1. Концепция Конвейера Обработки Запросов (Request Pipeline)
ASP.NET Core обрабатывает каждый входящий HTTP-запрос как последовательность шагов, или "конвейер". Каждый шаг в этом конвейере — это Middleware. Middleware может выполнить какую-то логику, а затем либо передать запрос следующему Middleware, либо сгенерировать ответ и остановить конвейер.

2. Разделение Ответственности (Separation of Concerns)
Вместо того чтобы встраивать логику логирования, замера времени или обработки ошибок непосредственно в каждый контроллер или каждое действие, эти "сквозные" задачи (cross-cutting concerns) вынесены в отдельные Middleware.

Контроллеры (BooksController): Сосредоточены исключительно на бизнес-логике: получение, добавление, валидация данных, специфичная для предметной области "Книги". Они не "знают" о том, как происходит логирование или форматирование ошибок.

Репозиторий (InMemoryBookRepository): Сосредоточен на логике доступа к данным, абстрагируя контроллеры от деталей хранения.

3. Middleware в действии:
RequestIdMiddleware:

Функция: Генерирует уникальный идентификатор (RequestId) для каждого входящего запроса. Логирует начало и окончание обработки запроса, а также метод и путь.

Преимущество: Обеспечивает сквозную трассировку. По RequestId можно отследить весь жизненный цикл запроса в логах, что критически важно для отладки и мониторинга в распределенных системах.

TimingMiddleware:

Функция: Измеряет общее время, потраченное на обработку запроса всей последующей частью конвейера (включая выполнение контроллера и репозитория). Результат логируется вместе с RequestId.

Преимущество: Помогает идентифицировать узкие места производительности в приложении.

ExceptionHandlerMiddleware:

Функция: Перехватывает все исключения, возникающие в последующих Middleware или в бизнес-логике контроллеров. Он преобразует эти исключения в стандартизированный JSON-ответ об ошибке (ErrorResponse) с соответствующим HTTP-статусом (400, 404, 500) и RequestId.

Преимущество: Обеспечивает единообразную обработку ошибок для клиентских приложений, упрощая их разработку. Предотвращает утечку чувствительной информации в производственной среде, раскрывая детали ошибок только в режиме разработки.

### Почему это аккуратный каркас, а не случайный набор функций:

Модульность и переиспользуемость: Каждый Middleware является независимым блоком, который можно легко добавить, удалить или переиспользовать в других проектах без изменения основной бизнес-логики.

Легкость сопровождения: Изменение логики логирования или формата ошибок требует изменения только одного Middleware, а не обхода всех контроллеров.

Чистота кода: Контроллеры остаются "тонкими" и сфокусированными на своей основной задаче, что делает их более читаемыми и легкими для тестирования.

Расширяемость: Добавление новых сквозных функций (например, аутентификации, авторизации, кеширования) реализуется путем добавления новых Middleware, не затрагивая существующую логику.

Надежность: Централизованная обработка ошибок гарантирует, что даже непредвиденные исключения будут преобразованы в дружественный для клиента ответ, а не приведут к падению сервиса или неинформативному ответу.
